export default function Home() {
  return (
    <main style={{ maxWidth: 760, margin: "72px auto", padding: "0 24px" }}>
      <h1>KIM-PER Meeting Scheduler</h1>
      <p>Slack command endpoint: <code>/api/slack</code></p>
      <p>This deployment manages recurring meetings, reminders and structured meeting records.</p>
    </main>
  );
}
